extends cafes

block active
	-var selected = 'Cafés';

block content
	.jumbotron.text-center
		h1 #{title}
		p #{content}
	h2
		if content
			#{content.wifi}
		else
			#{content}

	div#Information
		p#Name 
			if content
				#{content.wifi}
			else
				#{content}
		p#Type
		p#Wifi


